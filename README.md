# IsolationFinder for 2D DataSets

Build with Xcode or compile with gcc -objc

Execute it like:
./IsolationFinder <filepath>

The file must be a text file with a feature on each line like:

	<name> <coordinateX> <coordinateY> \n

e.g.:
	place0 876328 96192

